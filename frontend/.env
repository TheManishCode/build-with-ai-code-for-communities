VITE_API_BASE=http://localhost:8005
# NOTE: temporarily 8005, not 8000 -- port 8000 has a stuck orphaned socket in this
# environment (Get-NetTCPConnection shows it LISTENING under a PID that no longer exists;
# likely a Windows/WSL networking quirk from an earlier crashed process this session).
# Revert to 8000 once that clears (a machine restart should do it) if you'd rather keep
# the original convention.
